## 1. Módulo de constantes compartido

- [x] 1.1 Crear `src/renderer/src/components/views/sessionHeaderStyles.ts` exportando `SESSION_HEADER_CLASS` (`h-[72px] min-h-[72px] max-h-[72px] flex items-center justify-between mb-4`), `SESSION_TITLE_CLASS` (`text-lg font-bold text-slate-100 leading-tight truncate block`) y `SESSION_SUBTITLE_CLASS` (`text-xs text-slate-400 leading-normal truncate block`), según design.md (decisiones D1, D2, D4). Verificar: el archivo existe y `npx prettier --check src/renderer/src/components/views/sessionHeaderStyles.ts` no produce diffs.
- [x] 1.2 Verificar la compilación del nuevo módulo con `npm run typecheck` (debe permanecer limpio).

## 2. Cabeceras de las modalidades 01-03

- [x] 2.1 En `src/renderer/src/components/views/SingleNoteView.tsx` (cabecera de sesión, ~línea 147): sustituir la clase `h-14` por `SESSION_HEADER_CLASS`; reestructurar el bloque de título izquierdo en dos líneas — Línea 1 estática ("Discriminación de Altura Absoluta") con `SESSION_TITLE_CLASS` y Línea 2 dinámica con `SESSION_SUBTITLE_CLASS` (en sesión, `getSessionProgressLabel()`; en reposo, `N tonos activos` a partir de `trainer.activeNotes.length`); añadir `min-w-0` al bloque de título. Verificar: el JSX renderiza título y subtítulo en dos elementos `block` y la cabecera usa la clase compartida.
- [x] 2.2 En `src/renderer/src/components/views/IntervalsView.tsx` (~línea 66): mismo cambio que 2.1, con Línea 1 "Reconocimiento Interválico" y Línea 2 en reposo `N intervalos activos` a partir de `trainer.activeIntervals.length`, reutilizando `getSessionProgressLabel()`. Verificar: análogo a 2.1.
- [x] 2.3 En `src/renderer/src/components/views/SequencesView.tsx` (~línea 58): mismo cambio que 2.1, con Línea 1 "Memoria Melódica" y Línea 2 en reposo `N notas por frase` a partir de `trainer.sequenceLength`, reutilizando `getSessionProgressLabel()`. Verificar: análogo a 2.1.

## 3. Cabecera de la modalidad 04 (Repertoire)

- [x] 3.1 En `src/renderer/src/components/views/RepertoireView.tsx` (~línea 76): eliminar `flex-col sm:flex-row`, `items-start sm:items-center` y `gap-2`; aplicar `SESSION_HEADER_CLASS`; aplicar `SESSION_TITLE_CLASS` a la línea del título de la obra y `SESSION_SUBTITLE_CLASS` a la línea de metadatos (autor • métrica • BPM); añadir `min-w-0` al bloque de título y `shrink-0` al grupo de botones derecho. Verificar: la cabecera ya no cambia de altura al redimensionar el ancho (sin apilado responsivo) y conserva badge `MODO 04`, botón de partitura y botones de sesión.

## 4. Verificación de anclaje, formato y coherencia

- [x] 4.1 **Anclaje por construcción:** verificar con una búsqueda que `SESSION_HEADER_CLASS` aparece referenciada exactamente en los cuatro archivos de vista (SingleNoteView, IntervalsView, SequencesView, RepertoireView) y que ninguna retiene `h-14` ni `flex-col` en la cabecera. Las cuatro cabeceras quedan geométricamente idénticas → la posición Y del `PianoKeyboard` (mismo `<main>` en `App.tsx:547-589`, mismo `space-y-3` en la raíz de cada vista) es invariante al conmutar `appMode`.
- [x] 4.2 Formatear los archivos modificados: `npx prettier --write src/renderer/src/components/views/sessionHeaderStyles.ts src/renderer/src/components/views/SingleNoteView.tsx src/renderer/src/components/views/IntervalsView.tsx src/renderer/src/components/views/SequencesView.tsx src/renderer/src/components/views/RepertoireView.tsx` y verificar que el chequeo global de formato del proyecto no produce diffs.
- [x] 4.3 Ejecutar `npm run typecheck` y confirmar que queda limpio (sin errores ni en los archivos modificados).
- [x] 4.4 Ejecutar `npm run test` (vitest run, modo no interactivo) y confirmar que toda la suite existente pasa sin fallos ni nuevos saltos: no se añaden tests de layout (jsdom no computa geometría, y asertar la clase `h-[72px]` sería tautológico).
- [x] 4.5 Verificación visual manual a cargo del usuario en la ventana Electron: conmutar secuencialmente entre SingleNote → Intervals → Sequences → Repertoire y confirmar que el `PianoKeyboard` no se desplaza verticalmente (CLS = 0). No automatizable: el guardrail de Electron prohíbe `npm run dev` y la automatización de browser en tareas automatizadas; es el observable final del cambio. **EJECUTADA (2026-09-22):** el usuario confirmó que la cabecera (Nivel 1) queda anclada, pero detectó un desplazamiento residual de ~10-12 px producido por la barra de herramientas que está directamente sobre el piano (~30 px en las modalidades 01-03 vs ~42 px en Repertorio por el botón 'Metro Libre' y los LEDs de pulso). Ver sección 5.

## 5. Estandarización de la barra de herramientas sobre el piano (Nivel 2)

- [x] 5.1 Actualizar `SESSION_HEADER_CLASS` en `src/renderer/src/components/views/sessionHeaderStyles.ts` a `h-[72px] min-h-[72px] max-h-[72px] flex items-center justify-between mb-4`: el `max-h-[72px]` añade un tope duro además del `h`/`min-h` y el `mb-4` unifica el ritmo vertical de la cabecera en las cuatro vistas. Verificar: `npx prettier --check src/renderer/src/components/views/sessionHeaderStyles.ts` sin diffs.
- [x] 5.2 Aplicar a la barra de herramientas inmediatamente superior al piano en `SingleNoteView.tsx`, `IntervalsView.tsx` y `SequencesView.tsx` la clase fija `h-10 min-h-[40px] max-h-10 flex items-center justify-between mb-3` (conservando las clases tipográficas `text-xs font-mono text-zinc-400 px-1` ya presentes). Verificar: las tres barras quedan a exactamente 40 px con el mismo margen inferior antes de `<PianoKeyboard />`.
- [x] 5.3 En `RepertoireView.tsx`: aplicar esa misma clase a la barra (eliminando `flex-wrap sm:flex-nowrap`, cuyo envoltorio rompería la altura fija de 40 px); dar `h-8 shrink-0` al contenedor central del metrónomo y `h-8` al botón 'Metro Libre' (32 px, centrado verticalmente sin desbordar los 40 px de la barra); añadir `min-w-0` al `span` del título para que su `truncate` sea efectivo. Verificar: la barra mide 40 px y el bloque del metrónomo 32 px centrado, igual que en las demás modalidades.
- [x] 5.4 Ejecutar `npx prettier --write` sobre todos los archivos modificados, luego `npm run typecheck` (limpio) y `npm run test` (suite completa en verde).
- [x] 5.5 Re-verificación visual manual a cargo del usuario en la ventana Electron: conmutar de nuevo entre las cuatro modalidades y confirmar que el `PianoKeyboard` ya no se desplaza (CLS = 0 también en el Nivel 2). **CONFIRMADA (2026-09-22):** el usuario verificó en vivo que el piano permanece anclado al conmutar entre las cuatro modalidades.
